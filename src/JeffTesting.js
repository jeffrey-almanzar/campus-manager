import React from 'react';

import Add from './Add';

function Testing(props){
    return (
        <div>
            <Add name="Student" />
            <Add name="Campus" />
        </div>
    )
}

export default Testing;